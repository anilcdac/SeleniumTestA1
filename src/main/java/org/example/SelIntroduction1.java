package org.example;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class SelIntroduction1 {
    public static void main(String[] args){

        //Invoking Browser
        //Chrome - ChromeDriver -> Methods
        ChromeDriver driver = new ChromeDriver();
//        driver object here has access to all the methods of Chrome driver
//        if we use chromdriver, and want to change to firefox, chromedriver methods already written wont work with firefoxDriver
//        so better use WebDriver interface

//        WebDriver driver = new ChromeDriver();
//        driver object here has access to the methods of Chrome driver which are defined in web Driver Interface




    }

}
